import pandas as pd
from scholar_wizard import STATIC


# Snowball from a single study and return the results
def get_study_snowball_results() -> pd.DataFrame:
    pass
    return pd.DataFrame(columns=STATIC.STUDY_DF_COLUMNS)
